import { Layout, Banner, Section, Footer } from '../component';

function Home () {
    return (
        <Layout>
            <Banner />
            <Section />
            <Footer />
        </Layout>
    );
};

export default Home;