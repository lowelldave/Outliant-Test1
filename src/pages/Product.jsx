import { Layout, Footer, Products } from '../component';

function Product () {
    return (
        <Layout>
            <Products />
            <Footer />
        </Layout>
    );
};

export default Product;